# golf-with-friends
An app for Golf Leagues to monitor scoring, provide league information, and more.

## Docker Local
Stopping
`docker compose down ` add `-v` to remove the volumes
Starting
`docker compose up -d` `-d` detaches from the terminal and runs in the background